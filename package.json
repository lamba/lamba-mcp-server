{
  "name": "lamba-mcp-server",
  "version": "0.0.4",
  "description": "Lambda server for MCP learning resources",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {}
}
